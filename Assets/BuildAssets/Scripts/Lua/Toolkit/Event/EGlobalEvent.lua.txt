EGlobalEvent = {
    Proto = Enum.Index,
    TemplateModule = Enum.Index,
    Lanuch = Enum.Index,
    Login = Enum.Index,
    ViewEnter = Enum.Index,
    ViewExit = Enum.Index,
    GameStart = Enum.Index,
    GameOver = Enum.Index,
}

return EGlobalEvent